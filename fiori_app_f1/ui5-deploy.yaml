# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: com.consapiens.fiori.f1.fioriappf1
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: https://vhcers4dci.hec.elrosado.com:44300/
        app:
          name: ZFIORI_TESTJG
          description: Proyecto de prueba de fiori desde VSCode
          package: $TMP
          transport: ''
        exclude:
          - /test/
          - /localService/
